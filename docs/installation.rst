============
Installation
============

At the command line::

    $ easy_install team_metrics

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv team_metrics
    $ pip install team_metrics
